const cat = {
  name: "Midnight",
  age: 14,
};

// delete cat.name // if we do this before calling below, neither will print to console.

if (personal.name != null) {
  console.log("Has truthy name value");
} // won't print if property is false Idoes not have a value)

if ("name" in person) {
  console.log("Has name property");
} // prints if property is defined on "cat"
